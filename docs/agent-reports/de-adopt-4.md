# DE adoption slice 4

The real-cache shadow ledger is generated by `scripts/de-export/shadow-recipes.mjs`; its `deOnly.recipes`, `deOnly.resources`, and `deOnly.images` arrays are the exact DE-only record/path lists, while `mirrorOnly` proves that mirror records were not dropped.

Evidence from the read-only cache and preview export:

- Recipes: DE 1,885; mirror 2,005; merged 2,024; DE-only 19; mirror-only 139.
- Resources: DE 3,545; mirror 3,524; merged 3,571; DE-only 47; mirror-only 26.
- Images: DE Manifest 20,136; mirror image paths 12,467; merged 12,618; DE-only paths 151.
- Narin recipe: `/Lotus/Types/Recipes/WarframeRecipes/NarinBlueprint` → `/Lotus/Powersuits/Duelist/Duelist`.
- Narin dependency check: Narin, Narin Neuroptics, Narin Chassis, and Narin Systems all had a resolved name and a DE manifest image hash.

Validation performed without compilation: all three `tests/de-export/*.test.mjs` files passed, and `node --check` passed for every new JavaScript module. Rust was not compiled per task restrictions; `de_recipes.rs` was reviewed twice after editing.

Open risks: runtime verification of the Tauri refresh and Foundry rendering remains unavailable without running/building the app. The generated shadow JSON is intentionally outside the worktree; rerun the finite command above to refresh it from the approved cache.
