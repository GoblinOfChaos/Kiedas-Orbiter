The stable migration probe runner initially retained the historical ubuntu-stable-baseline-build.log destination because its filename replacement did not match the full path literal. Running the probe overwrote that historical debug build log. The new probe build log is preserved as stable-migration-probe-build.log; the runner now uses that unique destination. The earlier log is not reconstructed or claimed preserved. The prior stable executable was copied to before-stable-migration-probe/kiedas-orbiter before building, and historical package artifacts remain intact. This is evidence loss, not an app-source change.
